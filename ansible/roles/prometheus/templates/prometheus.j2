global:
  scrape_interval: 1s
  scrape_timeout: 1s

scrape_configs:
- job_name: envoy
  metrics_path: /stats/prometheus
  static_configs:
  - targets:
    - {{ groups['envoy'][0] }}:{{ envoy_admin_port }}
